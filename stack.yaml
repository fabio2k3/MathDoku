# ============================================================================
# MATHDOKU STACK CONFIGURATION (GLOSS COMPATIBLE)
# ============================================================================
resolver: lts-18.28  # GHC 8.10.7 - LAST gloss/OpenGL stable

packages:
- .

ghc-options:
  "$everything": "-Wall -O2"
